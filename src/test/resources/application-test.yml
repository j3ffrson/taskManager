spring:
  security:
    jwt:
      private:
        key: "test_secret_key_for_jwt_that_should_be_long_enough"
      user:
        generator: "test_generator"
      expiration:
        time: 3
    oauth2:
      client:
        registration:
          google:
            client-id: "GOOGLE_CLIENT_ID_TEST"
            client-secret: "GOOGLE_CLIENT_SECRET_TEST"
            scope:
              - openid
              - profile
              - email
  datasource:
    url: "jdbc:postgresql://localhost:5432/testdb"
    username: "test"
    password: "test"
